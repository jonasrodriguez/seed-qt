#include <gtest/gtest.h>

#include "DbManager.h"

TEST(TestDb, CreateDb) {
  // TODO
  ASSERT_EQ(1, 1);
  ASSERT_TRUE(true);
}

int main(int argc, char **argv) {
  testing::InitGoogleTest(&argc, argv);
  return RUN_ALL_TESTS();
}
